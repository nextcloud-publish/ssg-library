# Welcome

This is a sample **Collectives** export, used to try out `ssg-library`
locally. It exercises the main Markdown features the library supports.

## A fenced code block

```php
<?php
echo "hi from the sample site";
```

## A task list

- [x] Write sample pages
- [ ] Convince everyone to use static sites

## A multiline table

| Name    | Notes                          |
| ------- | ------------------------------- |
| Alice   | Likes long \
          descriptions           |
| Bob     | Short one                       |

![A sample image](.attachments.1/example.svg)

Nextcloud Text embeds every attachment as an image, even when it isn't one.
Those become plain links instead of broken images:

![notes.csv](.attachments.1/notes.csv)

Sub-pages are linked automatically below by `SiteBuilder`.
