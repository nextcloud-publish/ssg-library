# About

This is a leaf page: a `.md` file sitting next to `Readme.md`, rendered one
directory level below its folder (`about/index.html`).

It can contain any regular Markdown, e.g. a [link to CommonMark](https://commonmark.org/).
